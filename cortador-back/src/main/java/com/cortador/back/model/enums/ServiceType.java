package com.cortador.back.model.enums;

/**
 * Indica si el cortador lleva el jamón o lo pone el cliente.
 * Según esta opción, la reserva necesitará elegir un tipo de jamón (HamType) o no.
 */
public enum ServiceType {
    CUT_ONLY,
    FULL_SERVICE
}
